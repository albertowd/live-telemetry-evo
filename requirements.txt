PySide6>=6.7.0
# pyopenvr — SteamVR/OpenVR overlay output for the VR HUD. Ships the
# native openvr_api.dll inside the package; import is guarded so the
# overlay still runs on machines without it / without SteamVR.
openvr>=1.23.7
